export interface Account {
    id?: number,
    username?: string,
    firstName?: string,
    lastName?: string,
    role?: string,
    email?: string,
    isActive?: boolean,
    balance?: number,
}