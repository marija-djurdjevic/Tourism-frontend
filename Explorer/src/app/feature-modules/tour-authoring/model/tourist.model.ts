export interface Tourist {
    id?: number,
    username?: string,
    //email?: string,
    //isActive?: boolean
    firstName?: string; // Ime korisnika
    lastName?: string; // Prezime korisnika
}